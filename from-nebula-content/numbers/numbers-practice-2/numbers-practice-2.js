// Declare a variable sqrt and set it equal to the square root of 25
const sqrt = Math.sqrt(25);

// Declare a variable, power, and set it equal to 3 to the power of 5
const power = Math.pow(3, 5);

// Declare a variable, rounded, and set it equal to 7.4 rounded

const rounded = Math.round(7.4);

// Declare a variable roundedUp, and set it equal to the rounded-up value of 7.4

const roundedUp = Math.ceil(7.4);

// Declare a variable, absolute, and set it equal to the absoloute value of -13
const absolute = Math.abs(-13);

// Declare a variable, minimum, and set it equal o the minimum value in a set of numbers: 12,13,14,15,2,-1
const minimum = Math.min(12, 13, 14, 15, 2, -1);

// Declare a variable, maximum, and set it equal to the maximum value in a set of numbers: 12,13,14,15,2,-1
const maximum = Math.max(12, 13, 14, 15, 2, -1);

// Declare a variable, randomNum, and set it equal to a random number
const randomNum = Math.random();

// Declare a variable, zeroToTen, and set it equal to a random number between zero and ten
const zeroToTen = Math.random() * 10;

export {
  sqrt,
  power,
  rounded,
  roundedUp,
  absolute,
  minimum,
  maximum,
  randomNum,
  zeroToTen,
};
