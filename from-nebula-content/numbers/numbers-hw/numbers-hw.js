// 1. Declare a variable, myVar,  and assign it to a numeric value. Then half that value and re-assign it to the previous variable
let myVar = 12;

myVar = myVar / 2;

// 2. Create two variables, neg1 and neg2, and assign them to negative numbers negative numbers, then create a third variable, difference, and set it equal to neg1 subtracted from neg2
const neg1 = -6;
const neg2 = -12;
const difference = neg1 - neg2;

// 3. Create three variables, num1, num2, and num3, and assign numbers to them. Then, declare a third variable, multiplied, and set it equal to the previous three variables multiplied together.
const num1 = 2;
const num2 = 4;
const num3 = 6;
const multiplied = num1 * num2 * num3;

// 4. Declare two variables, even1 and even2, and assign them to even numbers. Then, declare a third variable, divideEven, and set it to even1 divided by even2.
const even1 = 24;
const even2 = 12;
const divideEven = even1 / even2;

// 5. Declare two variables, odd1 and odd2, and assign them to odd numbers. Then, declare a third variable, divideOdd, and set it to odd1 divided by odd2.
const odd1 = 9;
const odd2 = 3;
const divideOdd = odd1 / odd2;

// 6. Declare a variable, minVal and set it equal to the minimum value in the following set of numbers: 10,20,30,40,50

const minVal = Math.min(10, 20, 30, 40, 50);

// 7. Declare a variable, maxVal and set it equal to the maximum value in the following set of numbers:  -1,-2,-3,-4,-5
const maxVal = Math.max(-1, -2, -3, -4, -5);

// 8 .Create a variable, myNum and set it equal to a number, then declare a second variable, remainder, and set it equal to the remainder of myNum divided by 2.
const myNum = 79;
const remainder = myNum % 2;

// 9. Declare a variable squareRoot and set it equal to the square root of 144

const squareRoot = Math.sqrt(144);

// 10. Declare a variable, threeToSeven, and set it equal to the value of 3 to the power of 7
const threeToSeven = Math.pow(3, 7);

// 11. Declare a variable, rounded, and set it equal to the value of 73.2 rounded
const rounded = Math.round(73.2);

// 12. Declare a variable, roundedDown, and set it equal to the rounded-down value of 9.9
const roundedDown = Math.floor(9.9);

// 13. Declare a variable, absVal, and set it equal to the absolute value of -1231
const absVal = Math.abs(-1231);


// DO NOT EDIT BELOW THIS LINE
export {
  myVar,
  neg1,
  neg2,
  difference,
  num1,
  num2,
  num3,
  multiplied,
  even1,
  even2,
  divideEven,
  odd1,
  odd2,
  divideOdd,
  minVal,
  maxVal,
  myNum,
  remainder,
  squareRoot,
  threeToSeven,
  rounded,
  roundedDown,
  absVal,
};
