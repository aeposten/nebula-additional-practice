// 1. Declare a new variable, firstName, and set it equal to the string of your name
const firstName = "Amy";

// 2. Declare a variable, myNum,  and set it equal to a number
const myNum = 7;

// 3. Set a variable, myBoo, and set it equal to a boolean value
let myBoo = false;

// 4. Create a variable called carName, assign the value Volvo to it.
const carName = "Volvo";

// 5. Create a variable called num, assign the value 50 to it.
const num = 50;

// 6. Display the sum of 5 + 10, using two variables: num1 and num2.
const num1 = 5;
const num2 = 10;

console.log(num1 + num2);

// 7. Create a variable called num3, assign the addition of num1 and num2 to it.
const num3 = num1 + num2;

export { firstName, myNum, myBoo, carName, num, num1, num2, num3 };
